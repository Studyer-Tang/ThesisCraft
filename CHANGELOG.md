# 更新记录

## 4.0.6 — 2026-09-14

- 修复 Office 临时文档被占用时，清理错误覆盖排版结果、弹出 WinError 32 的问题；即使自动更新失败，也保留已生成的排版副本并给出更新提示。
- 临时文档改放系统临时目录，原稿旁仅发布结果；跨磁盘发布保持原始文件字节、原子替换和同名保护。
- 关闭临时文档失败时仍尝试退出辅助实例并写回错误结果；长文档自动更新等待上限由 3 分钟增加到 10 分钟。
- 增加真实 Windows 文件锁、Office 关闭异常、超时和文件发布回归测试。异常宿主仍可能持有系统临时文件，此时跳过清理，不关闭用户其他 Office 进程。
- Office 更新后补充公式和修订数量检查；内部格式改写允许通过，数量减少时保留更新前的排版副本。

## 4.0.5 — 2026-09-14

- Word/WPS 面板、论文工作台和通用桌面版新增输出文件夹选择，默认存放原稿旁，可一键恢复；插件打开论文设置时继承选择。
- 批量桌面排版默认分别保存到各自原文件所在目录；保留原命令行目录规划行为。失效目录明确提示，不会静默更换输出位置。
- 统一浅色界面、主按钮、文字层级和间距；收起模板管理次要操作，固定工作台底部保存与操作区，长设置页支持滚动。
- 插件增加处理进度及完成提示，插件与工作台支持打开结果文件夹。处理期间锁定目录选择；正常排版仍仅生成新的 DOCX。

## 4.0.4 — 2026-09-14

- 新增 32/64 位 Rust 原生 COM 功能区入口，正常启动 Word/WPS 后自动出现“学研排版”，按钮连接原有排版面板与模板设置。无需宏、管理员或 .NET CLR。
- 便携包新增一键安装和卸载原生入口；DLL 按内容哈希存放，升级不覆盖已加载的旧文件。保留桌面、CLI 和免注册外部入口。
- 更正旧实验诊断：Office 自动化模式可能跳过第三方加载项；正常启动时旧 .NET DLL 在本机触发 CLR 崩溃。停止推荐该入口，以原生 Rust 实现替代。
- 图片在最终页面设置后按本节、分栏和单元格可用宽度等比缩小，限制超高图片，适配同段并排图；独立图片居中并支持段前/段后与最大高度设置。
- 相邻中英文题注预留高度并与图片保持相邻；不将无关正文绑定为题注。保留小图尺寸、媒体数据和浮动图片位置。
- 增加图片边界、重复排版、媒体保留及原生 COM 身份/引用计数回归验证，CI 编译并测试两种 DLL 位数。

## 4.0.3 — 2026-09-14

- 修复包含原生脚注、尾注的文档应用注释样式时中断的问题；保留注释内容和引用标识。
- 修复固定行距裁切行内图片和较高公式的问题：含这些对象的段落改用最小行距，普通正文仍遵循模板。
- 增加脚注、尾注和行距回归测试，修正修订存在性检查中的 XML 元素判断方式。
- 长篇混排压力测试在本机 Word 上完成：163 页源稿完成排版，24 张图片、32 张表格（含嵌套表）、24 个原生公式、12 条批注、12 条脚注、6 条尾注和 8 对修订保留，1192 个内容标识无缺失。页数和耗时依赖宿主环境，不代表所有版面已符合学校规范。

## 4.0.2 — 2026-09-14

- 正常论文排版仅在原稿旁生成新的 DOCX，不再创建结果文件夹、模板快照或 HTML/JSON 报告。已有同名文件自动递增编号，原稿不覆盖。
- 工作台排版完成后直接打开生成文档；插件不再弹出报告网页。主动选择“只检查”仍可查看检查报告，主动导出 PDF 时才额外生成 PDF。
- Office 更新所用的临时文档、JSON 和临时 PDF 在处理后清理，更新异常仍保留排版副本并显示简短提示。

## 4.0.1 — 2026-09-14

- 修复 Word/WPS 关闭后取消事件订阅时出现“RPC 服务器不可用”的退出错误。
- 多个 Office 实例同时打开时，优先连接最近查看的可见文档，避免工具栏连接到另一个空白窗口。
- 新增独立的“学研排版”操作窗口，显示当前文档并支持手动重新连接；宿主隐藏传统工具栏时仍可操作。
- 修复自动连接检查窗口句柄的方式；未打开文档时提供明确提示，不额外启动空白文档。
- 新增多实例选择、连接失效与清理回归测试，以及真实宿主操作窗口验证脚本。

## 4.0.0 — 2026-09-13

- 发布 ThesisCraft（学研排版），作者 Study-Tang，GPT 辅助开发。
- 新增论文工作台、北大研究生与通用学位模板，支持中西文字体、字号和多种编号样式。
- 新增章节与附录编号、分节页码、目录、题注/公式与交叉引用、CSL 文献、三线表、续表和宽表横页。
- 新增论文骨架、前置页模板、符号表、章节合并拆分、格式检查报告和 PDF 导出。
- Windows Word/WPS 外部工具栏提供快速排版、设置、引用插入和域更新，可选登录后自动连接。
- 共用排版包取代重复实现，修复对象与格式丢失问题，保留桌面、CLI 与独立 Skill。
- Python 包使用统一模块入口启动插件，不再依赖仓库根目录脚本。

北大预设依据、文献标准版本及 Office 兼容边界见 README 和论文使用说明；标准 COM DLL 自动加载仍为实验功能。
