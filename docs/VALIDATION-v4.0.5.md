# 4.0.5 验证记录

日期：2026-09-14。作者 Study-Tang，GPT 辅助开发。

- 71 项 Python 单元测试通过，`ruff check word_formatter tests` 通过。
- 新增覆盖默认保存位置、自定义中文/空格目录、同名结果递增、原稿与已有文件保留、失效目录报错、批量文件分别在原稿旁保存，以及 Office 快速论文、PDF、通用排版和工作台启动参数中的目录传递。
- 在本机 Word 16.0 上从操作面板实际选择中文且包含空格的目录，执行快速论文排版；指定目录仅新增一个结果 DOCX（Office 临时锁文件除外），生成文件可读取并保留测试正文。结果自动打开，面板显示结果文件名。
- 从面板进入论文工作台，实际确认文档和所选目录正确带入。检查窗口显示：工作台底部保存位置和操作按钮固定可见，长设置区域可以滚动；插件主要按钮、工具区及结果入口完整可见。
- 原生功能区 DLL 沿用 4.0.4 的已验证版本，本次修改集中于独立操作面板、桌面工作台和输出目录传递；未声称重新验证全部宿主版本或所有论文版面。

正常排版只生成新 DOCX；主动“只检查”或勾选 PDF 时才生成相应报告或 PDF。文件夹选择仅保持在当前窗口，独立打开新窗口仍默认原稿旁；从插件打开工作台时继承本次选择。
